# Local overrides you do not want in git
values-local.yaml
*.local.yaml

# Rendered output from `helm template ... > out.yaml` while debugging
out.yaml
rendered/

# Editors / OS
.vscode/
.idea/
.DS_Store
Thumbs.db

# Python bytecode. app.py runs straight from the ConfigMap, but it also gets
# run locally during development - see the README.
__pycache__/
*.pyc
